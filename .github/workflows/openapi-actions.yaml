name: OpenAPI Github Actions
on: [push]
jobs:
  validate-contract:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - uses: readmeio/rdme@7.2.0
        with:
          rdme: openapi contract.yaml --key=flbEyOhwae2czobCJdFoX49ySg5ukHBF --id=contract --project-version=1.0